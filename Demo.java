import java.util.*;
import java.awt.*;

class Demo
{
	public static void main(String[] args)
	{
		Frame f[]=new Frame[50];
		Random r=new Random();
		Color obj=null;

		for(Frame fobj:f)
		{
            obj=new Color(r.nextInt(255),r.nextInt(255),r.nextInt(255))
			obj[i]=new Frame();
			obj[i].setSize(200,200);
			obj[i].setVisible(true);
			obj[i].setBackground(obj);
		}
	}
}